# k8s/overlays/local/configmap-patch.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: mcp-servers-config
  namespace: retail-ai-demo
data:
  # Point to your local or shared OpenAI-compatible endpoint when not using kServe.
  LLM_API_BASE: "http://127.0.0.1:8080/v1"
  MERIDIAN_DEBUG: "true"
  MILVUS_COLLECTION_NAME: "meridian_knowledge"
