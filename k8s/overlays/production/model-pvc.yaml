apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: granite-model-storage
  namespace: retail-ai-demo
  labels:
    app: granite-vllm
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
