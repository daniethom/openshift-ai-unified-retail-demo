# -----------------------------------------------------------------
# Milvus Vector Database Connection
# This should point to the Milvus service within your OpenShift cluster.
# The default service name is typically 'milvus-service'.
# -----------------------------------------------------------------
MILVUS_URI="http://milvus-service.meridian-demo.svc.cluster.local:19530"

# -----------------------------------------------------------------
# External Search API Credentials
# Your API key for the Tavily web search service.
# -----------------------------------------------------------------
TAVILY_API_KEY="***REMOVED***"

# -----------------------------------------------------------------
# LLM Endpoint
# The URL for your LLM service (e.g., Granite) served via kServe/vLLM.
# This needs to be the internal OpenShift service URL.
# -----------------------------------------------------------------
#GRANITE_ENDPOINT="http://your-llm-service.meridian-demo.svc.cluster.local:8000"
LLM_API_BASE="https://api.openai.com/v1" # Or your provider's base URL
LLM_API_KEY="***REMOVED***"
LLM_MODEL_NAME="gpt-4o-mini" # Or the model you wish to use

# -----------------------------------------------------------------
# Internal Agent & MCP Server Endpoints
# These URLs allow the agents to communicate with each other inside the cluster.
# The format is http://<service-name>.<namespace>.svc.cluster.local:<port>
# -----------------------------------------------------------------
HOME_AGENT_URL="http://home-agent-service.meridian-demo.svc.cluster.local:8000"
RAG_SERVER_URL="http://rag-server-service.meridian-demo.svc.cluster.local:8001"
SEARCH_SERVER_URL="http://search-server-service.meridian-demo.svc.cluster.local:8002"
ANALYTICS_SERVER_URL="http://analytics-server-service.meridian-demo.svc.cluster.local:8003"